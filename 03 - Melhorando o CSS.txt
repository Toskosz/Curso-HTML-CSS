* Vamos começar por fazer alterações em beneficios:
				<div class="conteudo-beneficios">
					<ul class="lista-beneficios">
						<li class="itens">Atendimento aos Clientes</li>
						<li class="itens">Espaço diferenciado</li>
						<li class="itens">Localização</li>
						<li class="itens">Profissionais Qualificados</li>
					</ul>

					<img src="beneficios.jpg" class="imagem-beneficios">
				</div>

* No CSS:
.conteudo-beneficios{
	width: 640px;
	margin: 0 auto;
}
.lista-beneficios {
	width: 40%;
	display: inline-block;
	vertical-align: top;
}
.imagem-beneficios{
	width: 60%;
}

* No html para o inline-block funcionar corretamente devemos tirar o espaço entre <ul> e o <img>

* Adicionaremos tambem a tag da section beneficios:
.beneficios {
	padding: 3em 0;
}

* Vamos fazer um pequena alteração no CSS do video ficando da seguinte forma:
.video {
	width: 560px;
	margin: 2em auto;
}

* Vamo tambem rearranjar de forma que o CSS de mapa fique antes dos beneficios 

* Agora devemos arrumar lista de beneficios a começar pelo contéudo que está faltando alguns itens, a lista ficara da seguinte forma:
					<ul class="lista-beneficios">
						<li class="itens">Atendimento aos Clientes</li>
						<li class="itens">Espaço diferenciado</li>
						<li class="itens">Localização</li>
						<li class="itens">Profissionais Qualificados</li>
						<li class="itens">Pontualidade</li>
						<li class="itens">Limpeza</li>
					</ul>

* Então devemos usar a classe 'itens' em nosso CSS:
.itens {
	line-height: 1.5;  ----> altura da linha
}

* Queremos então deixar a primeira linha da lista em negrito, poderiamos criar uma classe apenas para este primeiro item mas ai estariamos mudando no html algo que deveriamos mudar no CSS

* Então iremos usar pseudo-tags do CSS, da seguinte forma :
.itens:first-child {        ------> primeiro filho ou seja primeira aparição desta classe 
	font-weight: bold;
}

* além do first child temos o last-child e tambem o nth-child(n/xn), este ultimo podemos usar botando uma posição qualquer 'n' ou podemos usar em um operação, selecionando apenas numeros pares por exemplo com '2n' sendo x = 2

* Vamos agora aplicar o fundo degrade começando por criar a classe principal em nosso css:
.principal {
	padding: 3em 0;
	background: #FEFEFE;
}

* Em seguida adicionamos uma cor tambem a classe de beneficios:
.beneficios {
	padding: 3em 0;
	background: #888888;
}

* Percebemos então que da forma como centralizamos o conteudo, isto é, através da tag main no css não dara certo. Devemos então recortar o conteudo de mais e colocar nas respectivas classes:
.principal {
	padding: 3em 0;
	background: #FEFEFE;
	width: 940px;
	margin: 0 auto;
}

* Não entendi muito bem mas devemos criar um div ao redor do iframe do mapa de criarmos uma classe chamada mapa-conteudo:
.mapa-conteudo {
	width: 940px;
	margin: 0 auto;
}


* Vimos então as funções linear-gradient e a radial-gradient ambas com muitas propriedas as quais não vale a pena decorar 
.mapa {
	padding: 3em 0;
	background: linear-gradient(#FEFEFE, #888888);  ----> fica aqui porque é onde ocorre a transição
}

* Vamos agora dar uma olhada nos pseudo-elementos, por exemplo se quiser selecionarmos apenas a primeira letra de um titulo podemos fazer o seguinte:
.titulo-principal:first-letter {
	font-weight: bold;
}

* Podemos fazer o mesmo para uma linha de um paragrafo:
p:first-line {
	font-style: italic;
}

* Temos tambem os seguintes pseudo-elementos que eu nao entendi muito bem oque são só sei que eles são apenas de exibição e não sao manipulaveis pelo usuario:
.titulo-principal:before{
	content: "[";
}
.titulo-principal:after{
	content: "]";
}

* É justamente o pseudo-elemento que vimos acima que vamos usar para fazer a estrelas na lista do beneficios, ficando da seguinte forma:
.itens:before {
	content: "🟊";
}

* Quando queremos criar um elemento na página, via CSS, antes do elemento do HTML, usamos a propriedade :before. e :after para depois do elemento HTML

* Nesta aula, aprendemos:

    A melhorar mais ainda a semântica da página principal, com novas divisões, classes, etc
    Novas pseudo-classes
    Como aplicar um background gradiente na página
    Pseudo-elementos


